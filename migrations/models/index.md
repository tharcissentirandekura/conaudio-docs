# Models

Placeholder for documenting legacy model meaning and migration notes.

## Purpose

Use this page to explain what legacy models represent, where their source files live, and how each model maps to the conaudio2 target structure.

This is the home for notes that compare `server/app/models`, root legacy `app/models`, and `server/server/models`.

## To document later

- Legacy model files
- Persisted fields and computed fields
- Runtime behavior to preserve
- conaudio2 target files
- Migration checklist
