{
  "name": "@conaudio2/conaudio-docs",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "docs:dev": "vitepress --host 0.0.0.0",
    "docs:build": "vitepress build ",
    "docs:preview": "vitepress preview ",
    "deploy:dev": "npx netlify dev",
    "deploy:preview": "npx netlify deploy",
    "deploy:prod": "npx netlify deploy --prod"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  },
  "overrides": {
    "vite": "6.4.2"
  }
}
